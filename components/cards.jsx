import React from "react";
import "./cards.css"; // Add a CSS file for styling

const Cards = (props) => {
  return (
    <div className="cards-main">
      <div className="cards-content"> props data </div>
    </div>
  );
}

export default Cards;